# Sync No Bounce - Config

## debug
- `true`: write debug logs to `log_path`
- `false`: no file logs (recommended)

## log_path
Path to the log file. `~` is supported.
Example: `~/Downloads/anki_sync_no_bounce.log`
